// installing testing module called jest
// npm init
// npm i --save-dev jest

const add = (num1, num2) => num1+num2;
const myArray = ['dan', 'tom', 'ben']

module.exports = {
    add,
    myArray
}

